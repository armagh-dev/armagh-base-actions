[GENERATOR_SIGNATURE]

To add or modify actions:
1. Create a new action in the lib/client_actions/actions directory, using example_action.rb as a template (or replace example_action.rb)
2. Delete the provided example action
3. Update the gemspec file with any additional details.

** Please only modify files in the lib/client_actions/actions directory and the gemspec file **